[project]
name = "kmz-track-editor"
version = "0.1.0"
description = "两步路 KMZ 轨迹编辑器"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
kmz-editor = "kmz_editor.server:main"
